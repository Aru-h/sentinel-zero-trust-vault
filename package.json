{
  "name": "sentinel-zero-trust-vault-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "test": "npm run test --prefix frontend"
  }
}
